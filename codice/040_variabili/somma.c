#include <stdio.h>

int main() {
  int a, b, c;

  printf("Inserisci tre numeri interi\n");
  // input di a, b, c
  scanf("%d%d%d", &a, &b, &c);

  // stampa di a + b + c
  printf("La somma è %d\n", a + b + c);
}